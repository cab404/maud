<resources>

    <declare-styleable name="AppTheme">
        <attr name="main_background_color" format="color" />
        <attr name="drawer_background_color" format="color" />
    </declare-styleable>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <item name="drawer_background_color">@color/background_material_dark</item>
    </style>

</resources>
